```
(black formatter는 "가 기본, 다만 빠른 코드작성상 '도 고려 가능)
i = "hello"
i = f'''안녕하세요.
반갑습니다.
저는 유연우입니다
'''

상수 선언 enum ( from emum import Enum)
-> 그룹화 관리, 값 순회 가능

 [시퀸스 자료형(str, list, tuple) : index와 slicing 구현 가능  
 [순회 가능 자료형 : iter와 getitem 메서드를 구현 가능

소수점 계산 decimal (import decimal)

print('hello'  'world') = helloworld (+생략 가능)

print(s[-1]) : 실제로 음의 정수 인덱싱 X, __getitem__에서 양수 변환 후 인덱싱

s = 'Hello world!' -> 기본 : 순회가능한 string
			-> 심화 : 메서드(lower, upper)와 class가 있는 인스턴스

JS와 Python의 replace 다른점? : JS는 정규표현식 가능, Python은 re모듈 포함
(import re, *sub *findall 정규표현식은 좀 더 공부할 것)

(최근에 나온 것들)
{'one':1} | {'two':2} = {'one':1, 'two': 2}
	    -
	   &

튜플이 자주 사용되는 곳 : 불변 지키기 위해 < 아규먼트의 갯수를 맞추기 위해
```




